<h1 class="page-header">New Location</h1>
<div class="location-form">
  <%= form_for @location do |f| %>
    <%= render 'form', f: f %>
    <%= f.submit 'Create', :class => 'btn btn-primary right', data: {disable_with: 'Create...'} %>
  <% end %>
</div>
